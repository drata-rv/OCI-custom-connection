{
  "description": "Fails a backend set whose health status isn't OK (OCI's own enum: OK | WARNING | CRITICAL | UNKNOWN). Collapses AWS's separate latency/error-rate/unhealthy-host load balancer tests into one status check.",
  "evidenceType": "load_balancer_backend_set",
  "filteringCriteria": {
    "mode": "exclusion",
    "evaluator": { "all": [ { "fact": "evidenceType", "operator": "notEqual", "value": "load_balancer_backend_set" } ] }
  },
  "evaluator": {
    "all": [ { "fact": "backendSetHealthStatus", "operator": "equal", "value": "OK" } ]
  }
}
