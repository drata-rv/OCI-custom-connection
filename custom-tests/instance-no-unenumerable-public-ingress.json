{
  "description": "Fails a record whose public-source ingress rule is port-unrestricted or spans a genuine multi-port range -- too wide to enumerate as discrete ports in publicIngressPorts. A failure here means the instance's security rules need direct review.",
  "evidenceType": "instance",
  "filteringCriteria": {
    "mode": "exclusion",
    "evaluator": { "all": [ { "fact": "evidenceType", "operator": "notEqual", "value": "instance" } ] }
  },
  "evaluator": {
    "all": [ { "fact": "hasRangedPublicIngress", "operator": "equal", "value": false } ]
  }
}
