{
  "description": "Fails an autonomous database with a public endpoint hostname present. Mirrors AWS/Azure 'Database Public Endpoint Disabled'. NOT YET LIVE-TESTED -- no autonomous database exists in the free-tier tenancy this schema was validated against (2026-09-24). equal against a literal null is a confirmed real Drata operator (drata/multiverse Test Library recipe uses notEqual against null the same way), but this exact condition hasn't been pushed through the Advanced Editor. Confirm against a real ADB record before relying on it.",
  "evidenceType": "autonomous_database",
  "filteringCriteria": {
    "mode": "exclusion",
    "evaluator": { "all": [ { "fact": "evidenceType", "operator": "notEqual", "value": "autonomous_database" } ] }
  },
  "evaluator": {
    "all": [ { "fact": "publicEndpointHostname", "operator": "equal", "value": null } ]
  }
}
