{
  "description": "Fails a record whose public-source ingress rule is port-unrestricted or spans a genuine multi-port range -- too wide to enumerate as discrete ports in publicIngressPorts, so the collector flags it here instead of silently dropping it or listing partial ports. A true positive here means: go look at the instance's security rules directly. Not yet run through the Advanced Editor live.",
  "evidenceType": "instance",
  "filteringCriteria": {
    "mode": "exclusion",
    "evaluator": { "all": [ { "fact": "evidenceType", "operator": "notEqual", "value": "instance" } ] }
  },
  "evaluator": {
    "all": [ { "fact": "hasRangedPublicIngress", "operator": "equal", "value": false } ]
  }
}
