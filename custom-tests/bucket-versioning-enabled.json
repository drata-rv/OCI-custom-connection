{
  "description": "Fails a bucket whose versioning isn't Enabled (OCI's own enum: Enabled | Disabled | Suspended). Mirrors AWS/Azure 'Storage Data Versioned or Retained'.",
  "evidenceType": "bucket",
  "filteringCriteria": {
    "mode": "exclusion",
    "evaluator": { "all": [ { "fact": "evidenceType", "operator": "notEqual", "value": "bucket" } ] }
  },
  "evaluator": {
    "all": [ { "fact": "versioning", "operator": "equal", "value": "Enabled" } ]
  }
}
