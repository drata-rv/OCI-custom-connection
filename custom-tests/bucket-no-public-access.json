{
  "description": "Fails a bucket whose publicAccessType isn't NoPublicAccess (OCI's own enum: NoPublicAccess | ObjectRead | ObjectReadWithoutList). Mirrors AWS/Azure 'Cloud Storage Public Access Disabled'.",
  "evidenceType": "bucket",
  "filteringCriteria": {
    "mode": "exclusion",
    "evaluator": { "all": [ { "fact": "evidenceType", "operator": "notEqual", "value": "bucket" } ] }
  },
  "evaluator": {
    "all": [ { "fact": "publicAccessType", "operator": "equal", "value": "NoPublicAccess" } ]
  }
}
